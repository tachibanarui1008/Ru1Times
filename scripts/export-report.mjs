import { mkdir, writeFile } from "node:fs/promises";
import { pathToFileURL } from "node:url";
import path from "node:path";

const date = process.argv[2];
if (!/^\d{4}-\d{2}-\d{2}$/.test(date ?? "")) {
  throw new Error("Usage: node scripts/export-report.mjs YYYY-MM-DD");
}

const moduleName = `realReport${date.replaceAll("-", "")}`;
const reportModule = await import(pathToFileURL(path.resolve(`app/reports/${date}.ts`)));
const report = reportModule[moduleName];
if (!report || report.draft) throw new Error(`No formal report exported by ${moduleName}`);

const esc = (value = "") => String(value).replace(/[&<>"']/g, char => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[char]);
const nl = value => esc(value).replaceAll("\n", "<br>");
const link = (url, body) => url ? `<a href="${esc(url)}">${body}</a>` : body;
const segments = items => `<div class="segments">${items.map(item => `<span class="segment"><b>${esc(item.text)}</b><small>${item.reading ? `${esc(item.reading)} · ` : ""}${esc(item.romanization)}</small><em>${esc(item.zh)}</em></span>`).join("")}</div>`;

const css = `:root{color-scheme:light dark;--bg:#f7f7f4;--paper:#fff;--ink:#171916;--muted:#6d7068;--line:#d9dbd4;--accent:#1f5b45}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--bg);color:var(--ink);font-family:-apple-system,BlinkMacSystemFont,"Segoe UI","Noto Sans SC","Noto Sans JP","Noto Sans KR",sans-serif;line-height:1.75}.page{max-width:1080px;margin:auto;padding:56px 24px 90px}header.hero{border-top:2px solid var(--ink);padding:50px 0 58px}h1{font-size:clamp(42px,8vw,88px);letter-spacing:-.06em;line-height:.95;margin:0 0 24px}.deck{max-width:760px;font-size:19px;color:var(--muted)}.meta,.eyebrow,article>span,header>span,h5{font-size:12px;letter-spacing:.12em;text-transform:uppercase;color:var(--accent)}section{padding:58px 0;border-top:1px solid var(--line)}.section-head{display:flex;justify-content:space-between;gap:24px;align-items:end;margin-bottom:28px}.section-head h2{font-size:clamp(30px,5vw,52px);margin:0;letter-spacing:-.04em}.section-head p{color:var(--muted);margin:0}.story{padding:34px 0}.story h2{font-size:clamp(32px,5vw,60px);line-height:1.08;margin:12px 0}.story h3{font-size:24px;font-weight:500;margin:0 0 18px}.story p{max-width:780px;font-size:18px}.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px}.card,.briefing,.context,.challenge{background:var(--paper);border-radius:22px;padding:26px;border:1px solid var(--line)}.card h3{font-size:26px;margin:7px 0}.meaning{font-size:17px;font-weight:650}.card dl{display:grid;grid-template-columns:88px 1fr;gap:5px 12px}.card dt{color:var(--muted)}.card dd{margin:0}.card small{color:var(--muted)}blockquote,pre{margin:16px 0 0;padding:14px 16px;border-left:2px solid var(--accent);background:var(--bg);white-space:normal;font:inherit}.briefing{margin:18px 0}.briefing header{padding-bottom:20px;border-bottom:1px solid var(--line)}.briefing h3{font-size:28px;margin:8px 0 0}.briefing h4{font-size:19px;margin:4px 0;font-weight:500;color:var(--muted)}a{color:var(--accent);text-decoration:none}.language{padding:18px 0;border-bottom:1px solid var(--line)}.language:last-child{border:0}.language h5{margin:0}.segments{display:flex;flex-wrap:wrap;gap:14px 18px;padding:16px 0}.segment{display:grid;grid-template-rows:auto auto auto;text-align:center;min-width:54px}.segment b{font-size:16px}.segment small{font-size:11px;color:var(--accent)}.segment em{font-size:11px;color:var(--muted);font-style:normal}.deep{background:#18221d;color:#f4f5f1;margin-inline:calc(50% - 50vw);padding-inline:max(24px,calc((100vw - 1080px)/2));border:0}.deep .section-head p,.deep span{color:#9fc1b3}.standfirst{font-size:22px;max-width:820px}.pair{display:grid;grid-template-columns:1fr 1fr;gap:30px;padding:24px 0;border-top:1px solid #435149}.pair p{margin-top:6px}.context-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}.history{background:var(--paper);padding:30px;border-radius:24px}.timeline{display:grid;grid-template-columns:1fr 1fr;gap:24px}.challenge{margin:14px 0}.challenge li{padding:8px 10px}.challenge li.correct{color:var(--accent);font-weight:700}.sources{padding:0;list-style:none}.sources li{border-top:1px solid var(--line)}.sources a{display:grid;grid-template-columns:140px 1fr 110px;gap:16px;padding:16px 0}.sources small{text-align:right;color:var(--muted)}footer{padding-top:30px;color:var(--muted);font-size:13px}@media(max-width:720px){.page{padding:34px 18px 70px}.grid,.pair,.context-grid,.timeline{grid-template-columns:1fr}.section-head{align-items:start;flex-direction:column}.sources a{grid-template-columns:1fr;gap:3px}.sources small{text-align:left}.segments{gap:12px}.card,.briefing,.context,.challenge{padding:21px}}.ai-credit{width:min(1060px,calc(100% - 40px));margin:0 auto 90px;padding:24px 0;border-top:1px solid var(--line)}.ai-credit>span{color:var(--accent);font-size:8px;font-weight:700;letter-spacing:.12em;text-transform:uppercase}.ai-credit p{margin:10px 0 5px;font-size:12px}.ai-credit p strong{font-weight:650}.ai-credit small{color:var(--muted);font-size:10px;line-height:1.6}@media(prefers-color-scheme:dark){:root{--bg:#111310;--paper:#191c18;--ink:#f1f2ed;--muted:#a4a89f;--line:#343832;--accent:#8dc4a9}}`;

const html = `<!doctype html>
<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>小橘日报｜${esc(report.date)}</title><style>${css}</style></head><body><main class="page">
<header class="hero"><p class="eyebrow">第 ${String(report.edition_number).padStart(3, "0")} 期 · ${esc(report.date)}</p><h1>小橘日报</h1><p class="deck">四语全球晨报 · 每天约 ${report.estimated_minutes} 分钟，用中文、English、日本語、한국어理解同一个世界。</p><p class="meta">${report.hot_words.length} Hot Words · ${report.expressions.length} Expressions · ${report.briefings.length} Briefings · 1 Deep Read · ${report.challenge.length} Challenges</p></header>
<article class="story"><span>TODAY'S BIG STORY · ${esc(report.big_story.category)}</span><h2>${esc(report.big_story.title_en)}</h2><h3>${esc(report.big_story.title_zh)}</h3><p>${esc(report.big_story.summary)}</p>${link(report.big_story.source_url, `${esc(report.big_story.source_label)} ↗`)}</article>
<section><div class="section-head"><div><p>01 · VOCABULARY</p><h2>Hot Words</h2></div><p>今日热点词</p></div><div class="grid">${report.hot_words.map(word => `<article class="card word"><span>${esc(word.topic)}</span><h3>${esc(word.en)}</h3><p class="meaning">${esc(word.zh)}</p><dl><dt>日本語</dt><dd>${esc(word.ja)} <small>${esc(word.ja_romaji)}</small></dd><dt>한국어</dt><dd>${esc(word.ko)} <small>${esc(word.ko_romaja)}</small></dd></dl><h4>Why today?</h4><p>${esc(word.why_today)}</p><blockquote>${esc(word.example)}</blockquote></article>`).join("")}</div></section>
<section><div class="section-head"><div><p>02 · NATURAL LANGUAGE</p><h2>Expressions</h2></div><p>今日地道表达</p></div><div class="grid">${report.expressions.map(item => `<article class="card expression"><span>${esc(item.flag)} · ${esc(item.language)}</span><h3>${esc(item.phrase)}</h3>${item.romanization ? `<p class="romanization">${esc(item.romanization)}</p>` : ""}<p class="meaning">${esc(item.meaning)}</p><p>${esc(item.nuance)}</p><dl><dt>Scene</dt><dd>${esc(item.scene)}</dd><dt>Register</dt><dd>${esc(item.register)}</dd><dt>Frequency</dt><dd>${esc(item.frequency)}</dd></dl><pre>${nl(item.dialogue)}</pre><small>${esc(item.note)}</small></article>`).join("")}</div></section>
<section><div class="section-head"><div><p>03 · WORLD IN FOUR LANGUAGES</p><h2>Briefings</h2></div><p>四语全球简报</p></div>${report.briefings.map(item => `<article class="briefing"><header><span>${String(item.id).padStart(2, "0")} · ${esc(item.category)}</span><h3>${esc(item.title_zh)}</h3><h4>${esc(item.title_en)}</h4><p>${link(item.source_url, esc(item.source))} · ${item.minutes} min</p></header><div class="language"><h5>中文</h5><p>${esc(item.zh)}</p></div><div class="language"><h5>English</h5><p>${esc(item.en)}</p></div><div class="language"><h5>日本語</h5><p>${esc(item.ja)}</p>${segments(item.ja_segments)}</div><div class="language"><h5>한국어</h5><p>${esc(item.ko)}</p>${segments(item.ko_segments)}</div></article>`).join("")}</section>
<section class="deep"><div class="section-head"><div><p>04 · DEEP READ · ${esc(report.deep_read.category)}</p><h2>${esc(report.deep_read.title_en)}</h2></div><p>${report.deep_read.minutes} min</p></div><h3>${esc(report.deep_read.title_zh)}</h3><p class="standfirst">${esc(report.deep_read.standfirst)}</p>${report.deep_read.paragraphs.map((paragraph, index) => `<div class="pair"><div><span>English · ${String(index + 1).padStart(2, "0")}</span><p>${esc(paragraph.en)}</p></div><div><span>中文</span><p>${esc(paragraph.zh)}</p></div></div>`).join("")}</section>
<section><div class="section-head"><div><p>05 · CONTEXT</p><h2>看懂这件事</h2></div></div><div class="context-grid">${report.context.map((item, index) => `<article class="context"><span>${String(index + 1).padStart(2, "0")}</span><h3>${esc(item.title)}</h3><p>${esc(item.text)}</p></article>`).join("")}</div></section>
${report.history_lens ? `<section><div class="section-head"><div><p>06 · HISTORY LENS</p><h2>${esc(report.history_lens.title)}</h2></div></div><article class="history"><p>${esc(report.history_lens.text)}</p><div class="timeline"><div><strong>${esc(report.history_lens.then)}</strong><p>${esc(report.history_lens.similarities)}</p></div><div><strong>${esc(report.history_lens.now)}</strong><p>${esc(report.history_lens.differences)}</p></div></div></article></section>` : ""}
<section><div class="section-head"><div><p>07 · REVIEW</p><h2>Today's Challenge</h2></div><p>答案已展开，方便离线复习</p></div>${report.challenge.map(item => `<article class="challenge"><span>${String(item.id).padStart(2, "0")} · ${esc(item.kind)}</span><h3>${esc(item.question)}</h3><ol>${item.options.map((option, index) => `<li${index === item.answer ? ' class="correct"' : ""}>${esc(option)}</li>`).join("")}</ol><p><strong>答案：</strong>${esc(item.options[item.answer])}</p><p>${esc(item.explanation)}</p></article>`).join("")}</section>
<section><div class="section-head"><div><p>FURTHER READING</p><h2>延伸阅读</h2></div></div><ol class="sources">${report.sources.map(source => `<li><a href="${esc(source.url)}"><strong>${esc(source.label)}</strong><span>${esc(source.title)}</span><small>${esc(source.published)}</small></a></li>`).join("")}</ol></section>
<aside class="ai-credit"><span>EDITORIAL COLLABORATION</span><p>本期由橘瑠衣主编，与 <strong>${esc(report.ai_credit.provider)} ${esc(report.ai_credit.model)}</strong> 协作完成。</p><small>模型参与：${esc(report.ai_credit.role)}。内容取舍、观点表达与最终责任由作者承担。</small></aside><footer>小橘日报 · ${esc(report.date)} · 线上版：<a href="https://polyglot-morning-brief.tachibanarui1008.chatgpt.site">polyglot-morning-brief.tachibanarui1008.chatgpt.site</a></footer>
</main></body></html>`;

await mkdir("exports", { recursive: true });
const outputPath = path.resolve(`exports/${date}-小橘日报.html`);
await writeFile(outputPath, html, "utf8");
console.log(outputPath);
